export default class Slot {
  constructor(position, dimensions, bounds, emissive, id) {
    this.position = position;
    this.dimensions = dimensions;
    this.bounds = bounds;
    this.emissive = emissive;
    this.id = id;
  }
}